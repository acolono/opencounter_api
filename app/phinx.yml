paths:
    migrations: %%PHINX_CONFIG_DIR%%/db/migrations
    seeds: %%PHINX_CONFIG_DIR%%/db/seeds

environments:
    default_migration_table: phinxlog
    default_database: development
    production:
        adapter: mysql
        host: localhost
        name: production_db
        user: countapp
        pass: 'similarly-secure-password'
        port: 3306
        charset: utf8

    development:
        adapter: mysql
        host: opencounter-slim-codenv-mysql
        name: development_db
        user: docker
        pass: 'docker'
        port: 3306
        charset: utf8

    testing:
        adapter: mysql
        host: opencounter-slim-codenv-mysql
        name: development_db
        user: docker
        pass: 'docker'
        port: 3306
        charset: utf8
